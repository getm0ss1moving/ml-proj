# Changelog

## 2026-05-29

- Cleaned local runtime outputs, caches, logs, process markers, and temporary files from `/data/lry_machine_learning/lerobot`.
- Added project maintenance documentation under `docs/project/`.
- Extended `.gitignore` for local training and runtime artifacts so future runs do not pollute source control.
- Preserved the upstream LeRobot core code layout under `src/lerobot/`, `tests/`, `docs/source/`, and `examples/`.
